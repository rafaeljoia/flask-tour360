version: '3.8'

networks:
  dokploy-network:
    external: true

services:
  app:
    build: .
    container_name: bravia360
    restart: always

    networks:
      - dokploy-network

    volumes:
      - bravia360_uploads:/app/uploads

    deploy:
      labels:
        - "traefik.enable=true"
        - "traefik.docker.network=dokploy-network"

        # ROUTER
        - "traefik.http.routers.tour360.rule=Host(`tour360.braviaglobal.com.br`)"
        - "traefik.http.routers.tour360.entrypoints=websecure"
        - "traefik.http.routers.tour360.tls=true"

        - "traefik.http.routers.tour360.service=tour360"

        # SERVICE
        - "traefik.http.services.tour360.loadbalancer.server.port=5000"

volumes:
  bravia360_uploads:
    driver: local
